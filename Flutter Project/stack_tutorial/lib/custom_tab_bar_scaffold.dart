import 'package:flutter/material.dart';

class CustomTabBarScaffold extends StatelessWidget {
  const CustomTabBarScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
